<?xml version="1.0" encoding="utf-8"?>

<searchable xmlns:android="http://schemas.android.com/apk/res/android"
            android:label="string resource"
            android:hint="string resource"
            android:searchMode="queryRewriteFromText"
            android:inputType="text"
            android:imeOptions="actionSearch"
            android:searchSuggestAuthority="string"
            android:searchSuggestSelection="string"
            android:searchSuggestIntentAction="string"
            android:searchSuggestIntentData="string"
            android:searchSuggestThreshold="0"
            android:includeInGlobalSearch="false"
            android:searchSettingsDescription="Arsenal"
            android:queryAfterZeroResults="true"
            android:voiceSearchMode="launchRecognizer"
            android:voiceLanguageModel="free-form"
            android:voicePromptText="Arsenal Voice"
            android:voiceLanguage="English"
            android:voiceMaxResults="10" >

    <actionkey  android:keycode="KEYCODE_SEARCH"
                android:queryActionMsg="string"
                android:suggestActionMsg="string"
                android:suggestActionMsgColumn="string" >
    </actionkey>

</searchable>